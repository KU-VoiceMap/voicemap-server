name: Deploy API

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6

      - name: Build and deploy
        run: docker compose -f voicemap-api/docker/docker-compose.yml up -d --build --force-recreate
